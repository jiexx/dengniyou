app.controller('home', function ($scope, $rootScope, $location, $cookieStore, $http, $timeout, DATA) {
	$scope.$parent.showTitle = false;
	//ajaxPost($http, DATA, '/roles', function(resp){
	//	$scope.roles = resp;
	//});
});